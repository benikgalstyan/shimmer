import 'package:flutter/material.dart';

class BannerPlaceholder extends StatelessWidget {
  const BannerPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 320,
      height: 40.0,
      decoration: BoxDecoration(
          borderRadius: BorderRadius.circular(10.0),
          color: const Color(0xFFCFCFCF)),
    );
  }
}

class BannerPlaceholder2 extends StatelessWidget {
  const BannerPlaceholder2({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 294,
      height: 72.0,
      decoration: BoxDecoration(
          borderRadius: BorderRadius.circular(10.0),
          color: const Color(0xFFCFCFCF)),
    );
  }
}

class BannerPlaceholder3 extends StatelessWidget {
  const BannerPlaceholder3({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 233,
      height: 72.0,
      decoration: BoxDecoration(
          borderRadius: BorderRadius.circular(10.0),
          color: const Color(0xFFCFCFCF)),
    );
  }
}

class BannerPlaceholder4 extends StatelessWidget {
  const BannerPlaceholder4({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 258,
      height: 72.0,
      decoration: BoxDecoration(
          borderRadius: BorderRadius.circular(10.0),
          color: const Color(0xFFCFCFCF)),
    );
  }
}

class BannerPlaceholder5 extends StatelessWidget {
  const BannerPlaceholder5({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 282,
      height: 72.0,
      decoration: BoxDecoration(
          borderRadius: BorderRadius.circular(10.0),
          color: const Color(0xFFCFCFCF).withOpacity(1.0)),
    );
  }
}
